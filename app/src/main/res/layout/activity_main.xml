<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FAFAFA">
    <!-- simple row on top with add and delete buttons -->
    <LinearLayout
        android:id="@+id/top_controls"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center"
        android:padding="14dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- add new place button -->
        <Button
            android:id="@+id/btn_addCity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Add City" />

        <Space
            android:layout_width="22dp"
            android:layout_height="wrap_content"/>
        <!-- remove selected place button -->
        <Button
            android:id="@+id/btn_removeCity"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Remove City" />
    </LinearLayout>

    <!-- list view showing all the cities -->
    <ListView
        android:id="@+id/list_cities"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:divider="#BBBBBB"
        android:dividerHeight="1dp"
        app:layout_constraintTop_toBottomOf="@id/top_controls"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"/>
</androidx.constraintlayout.widget.ConstraintLayout>
